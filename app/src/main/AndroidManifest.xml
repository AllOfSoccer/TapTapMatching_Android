<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taptapmatching">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MatchingDetail.MatchingDetailActivity"></activity>
        <activity android:name=".MatchingListView" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".ButtonActivity2" />
        <activity
            android:name=".Button1Activity"
            android:label="@string/title_activity_button1"
            android:theme="@style/Theme.TapTapMatching.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="kakao2aaa5254b7023a6ff0cd23ee1a5a4dbc" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>